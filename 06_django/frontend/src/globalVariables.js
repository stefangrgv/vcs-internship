const DOMAINNAME = window.location.origin;
const SERVERPORT = 8000;
const SERVERADDRESS = `${window.location.protocol}//${window.location.hostname}:${SERVERPORT}`;

export { DOMAINNAME, SERVERADDRESS };
